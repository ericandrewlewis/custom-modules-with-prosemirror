{
  "name": "edit-article-with-prosemirror",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "scripts": {
    "test": "node test/start.js",
    "demo": "watchify -d -v --outfile demo/demo-built.js -t babelify demo/demo.js",
    "browsertests": "watchify -d --outfile demo/test-built.js -t babelify demo/test.js",
    "distBabel": "babel -d dist src",
    "distBrowserify": "browserify --outfile public/edit-bundle.js dist/edit.js",
    "dist-watch": "babel -w -d dist src",
    "lint": "blint --browser --ecmaVersion 6 --forbidSemicolons src"
  }
}
